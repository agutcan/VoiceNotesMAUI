<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VoiceNotesMAUI.NoteDetailPage"
             BackgroundColor="{StaticResource ColorFondo}">

    <VerticalStackLayout Padding="20" Spacing="20">

        <Border BackgroundColor="{StaticResource ColorTarjeta}"
        StrokeShape="RoundRectangle 12"
        StrokeThickness="0"
        Padding="20">

            <Label x:Name="EtiquetaNota"
           FontSize="18"
           LineBreakMode="WordWrap"
           TextColor="{StaticResource ColorTexto}" />

        </Border>


        <Button Text="🔊 Reproducir nota"
                FontSize="18"
                BackgroundColor="{StaticResource ColorVoz}"
                TextColor="White"
                CornerRadius="10"
                Clicked="OnReproducirNota" />

    </VerticalStackLayout>
</ContentPage>
